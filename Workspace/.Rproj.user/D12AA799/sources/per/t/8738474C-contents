rev <- read.csv("Revenus.csv")

# afficher les donnes
View(rev)

attach(rev)

# nuage de points
plot(x1, y1, pch = 19, main = "Nuage de points")










library(MASS)
data(package = "MASS")

data(GAGUrine)

bd <- GAGUrine
View(bd)
















